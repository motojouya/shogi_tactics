# Guidelines

## Knowledge
以下ディレクトリがある
- docs: 開発に必要な情報
- src: ソースコード
- test: 統合テストのテストコード

単体テストはコロケーションで、テスト対象ファイルと同じディレクトリに配置する。

## Development
開発に必要なコマンドはpackage.jsonにまとめる。  
開発の際は、ファイルを一つ一つ作成し、まとめて作成しない。

## Prohibitions
- 秘密情報を追加しない。生成コードにAPIキーや暗号鍵を埋め込まない。  
- コードの修正時にsed、perl、awk等のテキスト処理コマンドやスクリプトを使用しない。必ずeditツールを使用する。
- gitの操作は全面的に禁止する。

